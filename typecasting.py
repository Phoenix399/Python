name = "Nutifafa"
age = 11
is_student = False
for_sale = False
# Typecasting in Python
# Convert integer to string
age_str = str(age)
print(f"My age is " + age_str + ".")
age = float(age)

print(age)

age += "1"
# This will raise an error because you cannot concatenate a string to a float
# print(f"My age is " + age + ".")  # Uncommenting this line will raise an error    
# Convert string to integer

